spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sharxpenses_dev?useSSL=false&serverTimezone=UTC
    username: dev
    password: devpass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true

flyway:
  enabled: true
