spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sharxpenses_dev
    username: dev
    password: devpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # Flyway gerencia o schema, Hibernate não deve tocar
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    url: \
    user: \
    password: \
server:
  port: 8082

